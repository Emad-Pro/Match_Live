import 'package:flutter/material.dart';

class PlaylistScreen extends StatelessWidget {
  const PlaylistScreen({Key? key, required this.pathFile}) : super(key: key);
  final String pathFile;
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
